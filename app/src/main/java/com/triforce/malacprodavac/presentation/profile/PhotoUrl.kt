package com.triforce.malacprodavac.presentation.profile

import androidx.compose.ui.graphics.ImageBitmap

object PhotoUrl {
    var photoBitMap: ImageBitmap = ImageBitmap(1000,1000)
    var changed: Boolean = false
}